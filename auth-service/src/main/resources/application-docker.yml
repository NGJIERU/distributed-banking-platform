spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/banking_db
    username: banking_user
    password: secure_password

  data:
    redis:
      host: redis
      port: 6379

logging:
  level:
    com.banking.auth: INFO
    org.springframework.security: INFO
